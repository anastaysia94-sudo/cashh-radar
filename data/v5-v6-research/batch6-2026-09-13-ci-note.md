# Batch 6 CI expectations

This branch changes research/audit data and the V5/V6 checkpoint only. Existing application/runtime behavior should remain unchanged. CI must still pass the full Cashh Radar test workflow before merge.
