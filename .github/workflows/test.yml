name: Cashh Radar Tests

on:
  push:
    branches: [main]
    paths-ignore:
      - 'prospect-portal/**'
      - 'dist-v5-v6/**'
      - '**/*.md'
  pull_request:
    paths-ignore:
      - 'prospect-portal/**'
      - 'dist-v5-v6/**'
      - '**/*.md'
  workflow_dispatch:

concurrency:
  group: cashh-radar-tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          if [[ -f requirements.txt ]]; then
            python -m pip install -r requirements.txt
          fi
          python -m pip install pytest

      - name: Compile Python entrypoints
        run: |
          python -m py_compile launcher.py cashh_performance_radar.py api/index.py

      - name: Remove generated prospect portal from test workspace
        run: rm -rf prospect-portal

      - name: Run Python tests
        run: pytest -q

      - name: Validate deployment files and prospect PWA bundle
        shell: bash
        run: |
          set -euo pipefail
          test -f Dockerfile
          test -f render.yaml
          test -f deploy/render.yaml
          test -f vercel.json
          test -f railway.json
          test -f launcher.py
          test -f cashh_performance_radar.py
          test -d prospect-android-app
          test -f prospect-android-app/README.md

          grep -q '0.0.0.0' Dockerfile
          grep -q '\${PORT:-8000}' Dockerfile
          grep -q 'uvicorn launcher:app' Dockerfile
          grep -q '/api/health/ready' render.yaml
          grep -q '/api/health/ready' deploy/render.yaml
          grep -q 'api/index.py' vercel.json
          grep -q 'Dockerfile' railway.json
          grep -q '/api/health/ready' railway.json

          grep -q 'prospect' launcher.py
          grep -q 'performance' launcher.py
          grep -q 'scheduler' launcher.py
          grep -q 'threshold' cashh_performance_radar.py

          for f in index.html easy.html; do
            test -f "$f"
            grep -q 'loader\|bootstrap\|runtime' "$f"
          done

          test -f sw.js
          grep -q 'cashh-radar-prospects-v7-truthful-timing-20260912' sw.js
          grep -q '/api/' sw.js

          test -f prospect-android-app/bridge.js
          grep -q 'state' prospect-android-app/bridge.js
          grep -q 'performance' prospect-android-app/bridge.js
          grep -q 'Proposed.*100.*not revenue\|not revenue.*100' prospect-android-app/bridge.js

          test -f prospect-android-app/performance.html
          grep -q 'server' prospect-android-app/performance.html
          grep -q 'actual performance' prospect-android-app/performance.html || true

          test -f prospect-android-app/policy.json
          grep -q '"reddit"[[:space:]]*:[[:space:]]*false' prospect-android-app/policy.json
          grep -q '"emailOnly"[[:space:]]*:[[:space:]]*true' prospect-android-app/policy.json

          test -f prospect-android-app/manifest.json
          grep -q '"name"' prospect-android-app/manifest.json
          grep -q '"start_url"' prospect-android-app/manifest.json
          grep -q '"scope"' prospect-android-app/manifest.json

          python - <<'PY'
          import json
          from pathlib import Path
          for p in ['render.yaml','deploy/render.yaml','vercel.json','railway.json']:
              text = Path(p).read_text(encoding='utf-8')
              assert text.strip(), p
          json.loads(Path('vercel.json').read_text(encoding='utf-8'))
          json.loads(Path('railway.json').read_text(encoding='utf-8'))
          print('deployment config validation: OK')
          PY

          echo 'prospect portal is intentionally generated by the build workflow and excluded from CI workspace'

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Check frontend JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' f; do
            node --check "$f"
          done < <(find . -path './.git' -prune -o -path './node_modules' -prune -o -name '*.js' -type f -print0)

      - name: Check service worker JavaScript syntax
        run: node --check sw.js

      - name: Docker build smoke test
        run: docker build --pull -t cashh-radar-ci:smoke .
